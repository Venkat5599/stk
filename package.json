{
  "name": "stk",
  "private": true,
  "version": "0.1.0",
  "description": "Tell a new Solana program from a copy.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev:server": "pnpm --filter @stk/server dev",
    "dev:frontend": "pnpm --filter @stk/frontend dev"
  },
  "engines": { "node": ">=20" },
  "packageManager": "pnpm@10.0.0"
}
